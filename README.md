# fiap-arch-analyzer-api-gateway
API Gateway responsible for exposing and routing external requests to internal microservices.  Handles: - Request routing - Authentication/authorization integration - API composition (BFF pattern if applicable) - Entry point for diagram upload and status queries  Acts as the single entry point for clients interacting with the platform.
